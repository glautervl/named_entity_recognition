syntax = "proto3";

message InputMessage {
    string value = 1;
}

message OutputMessage {
    string value = 1;
}

service ShowMessage {
    rpc show(InputMessage) returns (OutputMessage) {}
}

service Recognize {
    rpc show(InputMessage) returns (OutputMessage) {}
}